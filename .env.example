# ===== 推荐：cc.freemodel.dev，只要一个 key =====
# cc 讲 Anthropic Messages 协议，鉴权只认 Bearer {FREEMODEL_API_KEY}，
# 不需要 CodeBuddy 登录、不需要装任何东西。代理负责把 OpenAI 协议翻译过去。
FREEMODEL_BASE_URL=https://cc.freemodel.dev/v1
FREEMODEL_TRANSPORT=cc_anthropic

# 代理同时开放 OpenAI（/v1/chat/completions）与 Anthropic（/v1/messages）
# 两种入站协议，客户端用哪种都行，共用同一份 PROXY_API_KEY 鉴权。

# 上游在容器内部注入了自己的 harness 提示词，客户端删不掉它。默认 true 会在
# 你的 system 之后追加一句陈述事实的话，压制它伪造工具调用标记的行为。
# 设成 false 就一个字都不加，只用客户端自带的提示词。
FREEMODEL_PROMPT_GUARD=true

# 必填：你的 Freemodel key（fe_... ）。
# 也可以留空启动后再执行：docker exec -it freemodel-proxy \
#   freemodel-workbuddy-proxy key set；它会写进 /data 卷，容器重建不丢。
FREEMODEL_API_KEY=

# 必填：代理自身的访问密钥，客户端用它调用本代理，而不是直接拿 Freemodel key。
# 它同时保护 Web 设定页 http://<主机>:40589/setup 的写入接口。
# 留空 = 关闭鉴权。镜像固定绑 0.0.0.0，所以留空会被拒绝启动，这是刻意的：
# 无鉴权的代理等于把你的 Freemodel 额度公开给所有能连上的人。
# 生成一个：openssl rand -hex 24
PROXY_API_KEY=

# 宿主机监听地址与端口（容器内固定 40589）。默认只绑 127.0.0.1；
# 要给局域网用就把 PROXY_BIND 改成 0.0.0.0，但必须先设好 PROXY_API_KEY。
PROXY_BIND=127.0.0.1
PROXY_HOST_PORT=40589

# 可用模型：claude-opus-5 / claude-fable-5 / claude-haiku-4-5-20251001。
# 其它 Claude 别名会被上游静默改换，代理按 message_start 回显真实后端名。
# 上游共享容器池占满时返回 500，代理会自动换后端重试。

# 预构建镜像的仓库路径。GHCR 只接受全小写，而仓库原名是
# MurasameCyan/Ciallo_Freemodel_Proxy，务必按下面这样写成小写。
IMAGE_REPO=murasamecyan/ciallo_freemodel_proxy

# ===== 备选：work.freemodel.dev，需要宿主机已登录的 WorkBuddy =====
# 把上面两行换成下面这一组才生效。该服务不能改成 http 直连。
# FREEMODEL_BASE_URL=https://work.freemodel.dev/v1
# FREEMODEL_TRANSPORT=workbuddy_acp
# WORKBUDDY_SIDECAR_MODE=external
#
# Compose 使用 host networking，因此可连接宿主机仅监听 127.0.0.1 的 gateway。
# WORKBUDDY_ACP_URL=http://127.0.0.1:44741
#
# 必填：宿主机 WorkBuddy 可访问的真实工作目录，不是容器内的 /workspace。
# Windows 示例：S:/AIWorker/Freemodel_Proxy/Ciallo_Freemodel_Proxy/workspace
# macOS 示例：/Users/your-name/Ciallo_Freemodel_Proxy/workspace
# Linux 示例：/home/your-name/Ciallo_Freemodel_Proxy/workspace
# WORKBUDDY_EXTERNAL_CWD=
#
# 仅当宿主机 gateway 开启 password 认证时设置。
# WORKBUDDY_ACP_PASSWORD=

# 说明：PROXY_PORT / PROXY_DEFAULT_PROJECT / PROXY_SESSION_STORE /
# PROXY_RUNTIME_DIR 已在镜像中设好默认值。
